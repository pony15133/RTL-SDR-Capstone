[pytest]
# Makes `import rtl_recorder` work when running `pytest` from this directory,
# without needing to `pip install -e .`.
pythonpath = .
markers =
    hardware: requires real RTL-SDR hardware attached - excluded by default.
addopts = -m "not hardware"
