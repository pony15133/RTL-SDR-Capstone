# The recorder itself uses only the Python standard library.
#
# Optional - accurate satellite pass prediction (rtl_recorder/passes.py).
# Without it a built-in Kepler+J2 model is used (pass times within ~10-30 s).
sgp4>=2.22
#
# Testing:
pytest>=7.0
